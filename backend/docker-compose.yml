version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/database:/app/database
      - ./backend/uploads:/app/uploads
    environment:
      - PYTHONUNBUFFERED=1